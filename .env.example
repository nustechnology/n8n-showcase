# openssl rand -hex 32 — generate once, then never change it.
# Encrypts every credential this n8n instance stores. Losing this key
# makes them unrecoverable, not just locked.
N8N_ENCRYPTION_KEY=
N8N_API_KEY=
